(ns semantics-interpreter.core-test
  (:require [clojure.test :refer :all ]
            [semantics-interpreter.core :refer :all ]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= (one) 2))))
